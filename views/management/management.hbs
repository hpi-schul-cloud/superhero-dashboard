{{#extend "lib/loggedin"}}

    {{#content "styles" mode="append"}}
        <link rel="stylesheet" href="/styles/events/events.css"/>
		<link rel="stylesheet" href="/styles/management/management.css"/>
    {{/content}}

    {{#content "scripts" mode="append"}}
		<script src="/scripts/management.js" type="text/javascript" defer></script>
		<script src="/scripts/base64files.js" type="text/javascript" defer></script>
    {{/content}}

    {{#content "page"}}
        <div class="container-fluid">
            <div class="row">
                <div class="col-sm-12 no-padding">
                    <a href="/management/releases" class="btn btn-primary">Releases fetchen</a>
                </div>
            </div>
			{{#ifEnvNot "SC_THEME" "n21"}}
				<div class="row">
					<section class="section-school section-default">
						<h2 class="h4">Zustimmungen</h2>
						<div class="container-fluid">
							<div class="row">
								<div class="col-sm-12 no-padding">
									{{> "lib/components/table" head=../policiesHead body=../policiesBody}}
								</div>
							</div>
							<div class="row">
								<div class="col-sm-12 no-padding">
									<button type="submit" class="btn btn-primary btn-add-modal--policy">
										Hinzufügen
									</button>
								</div>
							</div>
						</div>
					</section>
				</div>
			{{/ifEnvNot}}
        </div>
		{{#embed "lib/components/modal-form" action="/management/uploadConsent" method="post" class="add-modal--policy"}}
			{{#content "fields"}}
				{{> "management/forms/instance-policy"}}
			{{/content}}
		{{/embed}}
    {{/content}}
{{/extend}}
