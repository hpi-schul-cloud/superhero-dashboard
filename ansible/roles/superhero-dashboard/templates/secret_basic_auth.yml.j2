#jinja2: trim_blocks: "True", lstrip_blocks: "True"
apiVersion: v1
kind: Secret
metadata:
  name: shd-basic-auth-secret
  labels:
    app: shd
  namespace: {{ NAMESPACE }}
type: Opaque
data:
  auth: "{{ lookup('file', 'files/shd-'+stage+'-'+instance+'-passwd') | b64encode }}"
